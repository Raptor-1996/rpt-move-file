# RPT Move File 🚀

A powerful Python utility for moving files with root privileges on Linux systems.

## 📋 Features
- Move files with administrative privileges
- User-friendly CLI interface
- Automatic permission management
- File conflict resolution
- File information display
- Error handling and logging

## 🚀 Quick Start

### Prerequisites
- Python 3.x
- Linux OS (Kali Linux recommended)
- Root/sudo access

### Installation
```bash
# Clone repository
git clone https://github.com/Raptor-1996/rpt-move-file.git
cd rpt-move-file
```

# Make executable
```bash
chmod +x rpt_move.py

# Run with sudo
sudo python3 rpt_move.py
```

# Run installer script
```bash
chmod +x install.sh
./install.sh

# Now run from anywhere
sudo rpt-move
```

# Basic Usage
```bash
sudo python3 rpt_move.py
```
